# SyncSpeaker Server Configuration

# Server Port
PORT=8080

# Redis Configuration (required for multi-instance party discovery)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your-password-here  # Uncomment if using password auth

# Notes:
# - REDIS_HOST, REDIS_PORT, and REDIS_PASSWORD are optional
# - Defaults: localhost:6379 with no password
# - For production, set REDIS_PASSWORD and use a secure Redis instance
