# Phase 2 Architecture - Audio Upload & Streaming Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           PHASE 2 ARCHITECTURE                              │
│                  Audio Upload + HTTPS Streaming + Sync                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                                HOST (DJ)                                     │
└─────────────────────────────────────────────────────────────────────────────┘

1. SELECT FILE
   ┌───────────┐
   │ Choose    │
   │ Audio     │ → File selected: "track.mp3"
   │ File 🎵   │
   └───────────┘

2. UPLOAD TO SERVER
   ┌─────────────────────────────────────────────────────┐
   │ POST /api/upload-track                              │
   │ multipart/form-data { audio: <file> }              │
   │                                                     │
   │ ↓ Server processes:                                │
   │   - Validates audio/* MIME type                    │
   │   - Generates trackId: "ABC123XYZ"                 │
   │   - Saves to: /uploads/track-abc123.mp3           │
   │   - Stores in Map: uploadedTracks                 │
   │   - Returns: {                                     │
   │       ok: true,                                    │
   │       trackId: "ABC123XYZ",                        │
   │       trackUrl: "https://.../api/track/ABC123XYZ", │
   │       filename: "track.mp3"                        │
   │     }                                              │
   └─────────────────────────────────────────────────────┘

3. BROADCAST TRACK SELECTED
   ┌─────────────────────────────────────────────────────┐
   │ WebSocket → All Guests                              │
   │ {                                                   │
   │   t: "HOST_TRACK_SELECTED",                        │
   │   trackId: "ABC123XYZ",                            │
   │   trackUrl: "https://.../api/track/ABC123XYZ",     │
   │   filename: "track.mp3"                            │
   │ }                                                   │
   └─────────────────────────────────────────────────────┘

4. PLAY LOCALLY
   ┌───────────┐
   │ Play 🎵   │ → Audio plays from local file
   │ Button    │   (ObjectURL from file input)
   └───────────┘
        ↓
   ┌─────────────────────────────────────────────────────┐
   │ WebSocket → All Guests                              │
   │ {                                                   │
   │   t: "HOST_PLAY",                                  │
   │   trackId: "ABC123XYZ",                            │
   │   trackUrl: "https://.../api/track/ABC123XYZ",     │
   │   serverTimestamp: 1706918400000,                  │
   │   positionSec: 0                                   │
   │ }                                                   │
   └─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              GUEST (LISTENER)                                │
└─────────────────────────────────────────────────────────────────────────────┘

1. JOIN PARTY
   ┌───────────┐
   │ Enter     │
   │ Code:     │ → WebSocket connects
   │ ABC123    │
   └───────────┘

2. RECEIVE TRACK INFO
   ┌─────────────────────────────────────────────────────┐
   │ WebSocket ← Server                                  │
   │ {                                                   │
   │   t: "TRACK_SELECTED",                             │
   │   trackId: "ABC123XYZ",                            │
   │   trackUrl: "https://.../api/track/ABC123XYZ",     │
   │   filename: "track.mp3"                            │
   │ }                                                   │
   └─────────────────────────────────────────────────────┘
        ↓
   Stores: state.guestTrackUrl, state.guestTrackId

3. RECEIVE PLAY EVENT
   ┌─────────────────────────────────────────────────────┐
   │ WebSocket ← Server                                  │
   │ {                                                   │
   │   t: "PLAY",                                       │
   │   trackUrl: "https://.../api/track/ABC123XYZ",     │
   │   serverTimestamp: 1706918400000,                  │
   │   positionSec: 0                                   │
   │ }                                                   │
   └─────────────────────────────────────────────────────┘
        ↓
   Shows "TAP TO SYNC" overlay

4. USER TAPS TO SYNC
   ┌───────────────────┐
   │                   │
   │   🎵 Tap to      │
   │      Sync         │ → User gesture required
   │                   │   for browser autoplay
   └───────────────────┘
        ↓
   ┌─────────────────────────────────────────────────────┐
   │ SYNC CALCULATION                                    │
   │                                                     │
   │ 1. Compute elapsed time:                           │
   │    elapsedSec = (now - serverTimestamp) / 1000     │
   │                                                     │
   │ 2. Calculate target position:                      │
   │    targetSec = positionSec + elapsedSec            │
   │                                                     │
   │ 3. Load audio with Range request:                  │
   │    audio.src = trackUrl                            │
   │    → GET /api/track/ABC123XYZ                      │
   │                                                     │
   │ 4. Seek to position:                               │
   │    audio.currentTime = targetSec                   │
   │                                                     │
   │ 5. Play:                                           │
   │    audio.play()                                    │
   └─────────────────────────────────────────────────────┘

5. DRIFT CORRECTION (Every 5s)
   ┌─────────────────────────────────────────────────────┐
   │ While playing:                                      │
   │                                                     │
   │ 1. Calculate ideal position:                       │
   │    elapsedSec = (now - serverTimestamp) / 1000     │
   │    idealSec = startPositionSec + elapsedSec        │
   │                                                     │
   │ 2. Measure drift:                                  │
   │    drift = |audio.currentTime - idealSec|          │
   │                                                     │
   │ 3. Correct if needed:                              │
   │    if drift < 0.15s:  ✓ Good                       │
   │    if drift < 0.5s:   ⚠ Medium (nudge playback)    │
   │    if drift > 0.5s:   ❌ Poor (hard seek)          │
   │         audio.currentTime = idealSec               │
   │                                                     │
   │ 4. Update UI:                                      │
   │    Sync Quality: [Good / Medium / Poor]           │
   └─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          SERVER COMPONENTS                                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────┐
│ UPLOAD STORAGE                  │
│                                 │
│ uploadedTracks Map:             │
│ ┌─────────────────────────────┐ │
│ │ trackId → {                 │ │
│ │   filename,                 │ │
│ │   filepath,                 │ │
│ │   uploadedAt,               │ │
│ │   contentType,              │ │
│ │   sizeBytes                 │ │
│ │ }                           │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ FILE SYSTEM                     │
│                                 │
│ /uploads/                       │
│ ├── track-abc123.mp3            │
│ ├── track-xyz456.mp3            │
│ └── track-def789.mp3            │
│                                 │
│ TTL: 2 hours                    │
│ Cleanup: Every 5 minutes        │
└─────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ STREAMING ENDPOINT                                      │
│                                                         │
│ GET /api/track/:trackId                                 │
│                                                         │
│ Support HTTP Range Requests:                            │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Request Headers:                                    │ │
│ │   Range: bytes=0-1023                               │ │
│ │                                                     │ │
│ │ Response:                                           │ │
│ │   HTTP/1.1 206 Partial Content                      │ │
│ │   Content-Range: bytes 0-1023/5242880              │ │
│ │   Accept-Ranges: bytes                             │ │
│ │   Content-Type: audio/mpeg                         │ │
│ │   Content-Length: 1024                             │ │
│ │                                                     │ │
│ │   <binary audio data>                              │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ Required for:                                           │
│ - Audio seeking                                         │
│ - Mobile browser playback                               │
│ - iOS Safari compatibility                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ TTL CLEANUP JOB                                         │
│                                                         │
│ Runs every 5 minutes:                                   │
│                                                         │
│ 1. Check uploadedTracks Map                            │
│ 2. Find tracks older than 2 hours                      │
│ 3. Delete from filesystem:                             │
│    fs.unlinkSync(filepath)                             │
│ 4. Remove from Map:                                    │
│    uploadedTracks.delete(trackId)                      │
│                                                         │
│ Prevents disk space exhaustion                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          MID-TRACK JOIN SCENARIO                             │
└─────────────────────────────────────────────────────────────────────────────┘

Host plays for 30 seconds, then new guest joins:

1. Host: Playing at position 30s
   serverTimestamp = T0
   positionSec = 0

2. Guest joins 30s later (T0 + 30s)
   Receives: { serverTimestamp: T0, positionSec: 0 }

3. Guest taps to sync:
   elapsedSec = (now - T0) / 1000 = 30
   targetSec = 0 + 30 = 30s
   audio.currentTime = 30s ✓

4. Audio starts at correct position!

┌─────────────────────────────────────────────────────────────────────────────┐
│                          SYNC QUALITY INDICATORS                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌────────────┬──────────────┬────────────────┬─────────────────────┐
│ Drift      │ Quality      │ Action         │ User Feedback       │
├────────────┼──────────────┼────────────────┼─────────────────────┤
│ < 0.15s    │ ✓ Good       │ None           │ 🟢 Good             │
│ 0.15-0.5s  │ ⚠ Medium     │ Playback nudge │ 🟡 Medium           │
│ > 0.5s     │ ❌ Poor      │ Hard seek      │ 🔴 Poor             │
└────────────┴──────────────┴────────────────┴─────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                          RAILWAY DEPLOYMENT NOTES                            │
└─────────────────────────────────────────────────────────────────────────────┘

⚠️  EPHEMERAL STORAGE
    - Files stored in /uploads/ (container filesystem)
    - Lost on: redeploy, restart, scaling
    - TTL: 2 hours maximum

✅  WORKS FOR PROTOTYPE
    - Short-lived parties (< 2 hours)
    - Single-instance deployment
    - Limited concurrent users

🔄  PRODUCTION MIGRATION PATH
    - Move to S3/Cloudflare R2
    - Add CDN for global distribution
    - Persistent storage
    - Multi-instance compatible

See RAILWAY_STORAGE_NOTES.md for full details.
